DICE - это комплексный инструмент разработки для создания настольных 2D-игр. Система строится на принципе отделения игровой логики от графического представления, что позволяет пользователю создавать новые игры через конфигурационные файлы без пересборки всего приложения.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
chmod +x setup_libraries.sh
./setup_libraries.sh
```

### 2. Сборка

```bash
mkdir -p build && cd build
cmake ..
make -j$(nproc)
```

### 3. Запуск

```bash
./dice
```

## 📦 Что установится

- **SFML** - графика, сеть, аудио, окна
- **Lua 5.3** - скриптовый движок
- **Dear ImGui** - пользовательский интерфейс
- **nlohmann/json** - работа с JSON
- **sol2** - C++ биндинги для Lua
- **spdlog** - логирование
- **Google Test** - тестирование

## 🛠️ Настройка VSCode

### Создайте `.vscode/` папку и скопируйте конфиги:

```bash
mkdir -p .vscode
cp vscode_settings.json .vscode/settings.json
cp vscode_tasks.json .vscode/tasks.json
cp vscode_launch.json .vscode/launch.json
```

### Установите расширения:
- C/C++ (Microsoft)
- CMake Tools
- C/C++ Extension Pack

### Горячие клавиши:
- `Ctrl+Shift+B` - сборка проекта
- `F5` - запуск с отладкой
- `Ctrl+F5` - запуск без отладки

## 📁 Структура проекта

```
DICE/
├── CMakeLists.txt          # Конфигурация сборки
├── scr/                    # Исходный код
│   ├── core/              # Основная логика
│   ├── editor/            # Редактор игр
│   └── main.cpp           # Точка входа
├── include/               # Заголовочные файлы
│   └── core/
├── external/              # Внешние библиотеки
├── asset/                 # Игровые ресурсы
├── scripts/               # Lua скрипты
├── tests/                 # Тесты
└── build/                 # Папка сборки (создается автоматически)
```

## 🎮 Особенности

- ✅ 2D графика (SFML)
- ✅ Lua скриптинг для логики игр
- ✅ JSON конфигурация
- ✅ Визуальный редактор
- ✅ Сетевая игра (опционально)
- ✅ Кроссплатформенность (Linux/Windows/Mac)

## 🧪 Запуск тестов

```bash
cd build
./dice_tests
```

или

```bash
cd build
ctest --output-on-failure
```

## 🐛 Возможные проблемы

### SFML не найдена
```bash
sudo apt install libsfml-dev
```

### Ошибки с Lua
```bash
sudo apt install lua5.3 liblua5.3-dev
```

### OpenGL не найден
```bash
sudo apt install libgl1-mesa-dev libglu1-mesa-dev
```

## 👥 Команда

- Юмашев Артур
- Дьяков Леонид
- Ильин Ярослав


## 🔗 Полезные ссылки

- [SFML Documentation](https://www.sfml-dev.org/documentation/)
- [Lua 5.3 Manual](https://www.lua.org/manual/5.3/)
- [Dear ImGui](https://github.com/ocornut/imgui)
- [nlohmann/json](https://json.nlohmann.me/)
- [sol2](https://sol2.readthedocs.io/)
